# frozen_string_literal: true
class WelcomeController < ApplicationController
  def index
    # This action should list users budgets
    # if current_user
    #   @plans = BudgetPlans.all
    #   respond_to do |format|
    #     format.html
    #     # as_json or to_json??
    #     format.json { render(json: @plans.as_json(only: %i[id f_name l_name email user_name])) }
    #   end
    # end
  end
end
